pipeline {
    agent any
        stages {
        stage('clone') {
            steps {
                echo "cloning is done"
            }
        }
        stage('build') {
            steps {
                echo "building is done"
            }
        }
         stage('deploy') {
            steps {
                echo "deployed to artifactory"
            }
        }

    }
}
